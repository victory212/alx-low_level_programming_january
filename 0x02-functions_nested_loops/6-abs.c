#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: the int to check
 * Return: the ansolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
	return (0);
	}
	else
	{
	return (-n);
	}
}
