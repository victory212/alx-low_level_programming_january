#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @r: the int to check
 * Return: the ansolute value of int
 */
int _abs(int r)
{
	if (r >= 0)
	{
	return (r);
	}
	else
	{
	return (-r);
	}
}
